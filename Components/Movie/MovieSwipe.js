import React from 'react';
import { View, StyleSheet, Text } from "react-native";

const MovieSwipe = () => {
  return (
    <View style={styles.container}>
      <Text>Welcome on page Home</Text>
    </View>
  )
}

const styles = StyleSheet.create({
  container:{}
})

export default MovieSwipe;
