import React from 'react';
import { View, StyleSheet, Text } from "react-native";

const SearchMovie = () => {
  return (
    <View style={styles.container}>
      <Text>Welcome on page Search</Text>
    </View>
  )
}

const styles = StyleSheet.create({
  container:{}
})

export default SearchMovie;
