import React from 'react';
import { View, StyleSheet, Text } from "react-native";

const Profile = () => {
  return (
    <View style={styles.container}>
      <Text>Welcome on page Profile</Text>
    </View>
  )
}

const styles = StyleSheet.create({
  container:{}
})

export default Profile;
